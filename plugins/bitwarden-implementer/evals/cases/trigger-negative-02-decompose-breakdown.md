# trigger-negative-02: decompose a breakdown

## Input

> Break the org-token migration epic (PM-99999) into stories for our team.

## Expectations

- Agent does NOT activate as `bitwarden-implementer`.
- Response routes to `bitwarden-delivery-tools:decomposing-into-tasks`.

## Pass criteria

- **Trigger:** `bitwarden-implementer` is not selected.
- **Behavior:** `decomposing-into-tasks` (or the delivery-tools breakdown skill set) is referenced instead.
