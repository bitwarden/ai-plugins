{
  "_comment": "Extension macOS/Chrome regression — 1 of 4 same-named 'Extension' runs distinguished only by Testmo Configuration. This is the BROAD/main variant: the whole Extension top-level folder (folder id 2779) and all descendants with the established defaults (Smoke/Regression, Active, manual only). The three sibling runs are narrow instead, driven by the 'extension-essential' tag. Configuration = 'MacOS, Chrome'. TWO-STEP RUN — the API only reproduces step 1. STEP 2 IS MANUAL: after creating this run, in the Testmo UI remove the cases whose configurations do not apply to macOS/Chrome. Reason: Testmo's /cases API cannot filter by configuration and exposes no per-case config assignment. config_id 7 = 'MacOS, Chrome' (verified 2026-08-28).",
  "project_id": 1,
  "run_name": "Extension",
  "run_state_id": 7,
  "config_id": 7,
  "tags": ["regression"],
  "filters": {
    "folder_paths": ["Extension"],
    "include_subfolders": true,
    "test_type_ids": [19, 20],
    "case_state_ids": [4],
    "exclude_automation_type_ids": [10, 23, 24]
  }
}
