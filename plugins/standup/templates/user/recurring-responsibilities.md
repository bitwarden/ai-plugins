## Recurring responsibilities

Optional. Use this section to flag recurring work that would otherwise look like
noise, so the report represents it as real work rather than collapsing it into
the routine tail. Delete this section if you have nothing recurring.

- [YOUR-PREFERENCE] — name a recurring responsibility and how it shows up in your activity, so the skill treats it as substantive rather than dismissing it. For example: "Recurring release-branch cuts. The automated version-bump PRs on our main repositories are a real, scheduled release responsibility — represent them as release work (e.g. 'cut the recurring release branches'), not as dependency-bot noise to drop into the routine tail."
